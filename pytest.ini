[pytest]
markers =
    scenario: deterministic scenario-matrix tests for server operations
    fuzz: deterministic seeded fuzz/property tests for server operations
    fuzz_heavy: extended seeded fuzz/property tests (opt-in)
    gui_smoke: lightweight GUI startup smoke checks

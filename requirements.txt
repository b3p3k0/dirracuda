# Dirracuda Dependencies
# Combined dependencies for CLI and GUI components

# Core networking and SMB libraries (CLI)
shodan>=1.25.0
smbprotocol>=1.10.0
pyspnego>=0.8.0
PyYAML>=6.0             # YAML parsing for RCE signatures

# GUI-specific dependencies
ttkthemes>=3.2.0         # Better tkinter styling
Pillow>=8.0.0            # Image handling for icons
python-dateutil>=2.8.0   # Date parsing utilities
impacket>=0.11.0         # SMB probe enumeration support
pyasn1>=0.6.3            # Security fix: avoid vulnerable recursion DoS in <=0.6.2
pyOpenSSL>=26.0.0        # Security fix: reject oversized DTLS cookie callback values
cryptography>=46.0.6     # Security fix: name constraints peer-name validation bypass in <46.0.6
requests>=2.33.0,<3.0.0  # Security fix: avoid predictable temp extraction path in <2.33.0
# Pygments is intentionally not included in runtime requirements.
# Current advisory for <=2.19.2 has no patched release; project runtime does not import it.
# Keep Pygments/pytest limited to isolated local test environments.

# Database and data handling (included in Python standard library)
# sqlite3 - built-in
# tkinter - usually included with Python
# json - built-in
# csv - built-in
# logging - built-in
# argparse - built-in
# datetime - built-in
# os, sys, subprocess - built-in
# threading - built-in
# typing - built-in
# pathlib - built-in
# contextlib - built-in
# collections - built-in
# uuid - built-in
# socket - built-in
# time - built-in
# re - built-in
# glob - built-in
# shutil - built-in
# struct - built-in
# io - built-in
